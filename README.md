# SimpleBackendSetup
FastAPI + PostgreSQL + Docker + AWS Setup

This repo contains boilerplate code for a simple backend site/app that uses a common backend framework. Mainly used to refresh my knowledge and have something I can quickly duplicate for other projects.
